# 部署方案（zidongfabu 原项目笔记）

> 本方案用于在新电脑上复原本项目；**每次优化/修复后必须同步更新本文件**。
> 公开仓库不含 runtime/wheels/cache/matrixmedia 安装包等大件，克隆后请先按 `DEPLOY.md` 获取运行环境（或直接用系统 Python）。

## 1. 项目简介

多平台视频自动发布调度系统：监听任务文件夹（`watch\`），把 AI 流水线产出的视频自动分发到
抖音、快手、B站、头条、百家号、视频号、小红书、番茄视频等平台。
- 自研调度服务（本仓库）：监听、去重、重试、定时、日志、结果记录（SQLite）
- MatrixMedia 矩媒（开源 GPL-2.0）：各平台网页端自动化上传，通过 HTTP API 驱动，首次运行自动安装
- 整个目录复制到 Windows 新电脑即可运行（runtime\ 便携 Python + wheels\ 离线依赖）

## 2. 新电脑要求

- Windows 10/11 64 位；无需联网安装依赖（离线 wheels）
- MatrixMedia 自动化上传依赖浏览器环境（Chrome/Edge），各平台登录态保存在本机

## 3. 目录结构

- `scheduler\`：调度服务源码；`scripts\`：自测/推送脚本；`examples\`：任务模板
- `runtime\`：便携 Python；`wheels\`：离线依赖；`matrixmedia\`：矩媒安装包
- `watch\`：待发布任务；`pending\`：处理中/定时等待；`success\`：成功（含 duplicate）；`fail\`：失败
- `logs\`：按天日志；`data\`：SQLite/pid/停止标记；`config.yaml`：全部配置

## 4. 启动方法

- **一键启动**：双击 `start.bat`（自动检查环境、安装/启动 MatrixMedia、后台启动调度服务）
- **一键关闭**：双击 `stop.bat`
- **环境修复/重新部署**：双击 `install.bat`
- **配置**：`config.yaml` 配置账号别名（如 `dy-01: "13800138000"`）、API 地址与端口

## 5. 使用与验证

- MatrixMedia 窗口登录各平台账号（每平台一次）
- 把任务文件夹放入 `watch\`（`video.mp4` + 可选 `cover.jpg` + `info.json`，模板见 examples\）
- 验证：`success\` 出现发布成功记录（含去重 duplicate）；`logs\` 查看日志；`fail\result.json` 排查失败

## 6. 参考文档

- `README.md`（目录/任务格式/快速开始）；`DEPLOY.md`（新机器部署）；`docs\使用说明.md`（详细使用说明）

## 7. 更新约定

每次优化/修复后同步更新本文件。
