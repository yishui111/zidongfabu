# 项目速览：多平台视频自动发布（zidongfabu）

> 给 AI 助手看的项目备忘。需要细节时再读 `README.md` / `docs\使用说明.md`，不要全盘扫描。

## 这是什么

把 AI 流水线生成的视频任务文件夹自动分发到抖音、快手、B站、头条、百家号、视频号、小红书、番茄视频等平台。

## 两部分配合结构（核心）

| 部件 | 路径 | 职责 |
|------|------|------|
| MatrixMedia 矩媒（开源 GPL-2.0） | `matrixmedia\` | 负责各平台网页端的自动化上传发布，通过内置 HTTP API 驱动（独立进程，不修改其代码） |
| 自研调度服务（本仓库 MIT） | `scheduler\` | 监听任务文件夹，逐平台调用 MatrixMedia，负责去重/重试/定时/结果记录/日志 |

## 技术形态

- Python 3.12 + 内嵌便携运行时 `runtime\`（目标机无需装 Python）+ 离线 wheels
- 配置在 `config.yaml`（平台账号别名、重试、间隔等）
- 复制整个目录到另一台 Windows 电脑即可运行（首次运行自动静默安装 MatrixMedia）

## 工作流程

```
watch\任务文件夹\（video.mp4 + 可选 cover.jpg + info.json）
  → pending\（处理中/定时）→ 调 MatrixMedia 逐平台发布
  → success\（成功，含 duplicate 重复目录）/ fail\（失败，附 result.json）
```

- `info.json`：title / platforms（平台代码）/ account（账号别名）/ tags / publish_at（定时）
- 单线程串行发布，平台间默认间隔 5 秒；每平台最多重试 3 次；视频按 MD5 去重

## 启动/测试

- `start.bat`：一键启动（装/启 MatrixMedia + 后台调度）；`stop.bat`：停止；`install.bat`：环境修复
- 自测（不依赖真实账号/网络，模拟 API）：`scripts\run_tests.bat`

## 与兄弟项目的关系

- **上游**：`aishengchenshipin`（AI 视频生成）→ 生成视频后按任务文件夹格式放入 `watch\` 即自动发布
- 账号/Cookie/日志/数据库等敏感数据不入库

## 状态

- 可运行，已有 GitHub 仓库管理（`.git\`）
